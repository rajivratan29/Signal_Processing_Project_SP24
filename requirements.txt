```pip-requirements
# --- Core numerical / signal processing ---
numpy==2.0.2
scipy==1.13.1
matplotlib==3.9.4

# --- App front end ---
streamlit==1.59.2
plotly==6.7.0

# --- Audio I/O (core/audio.py, main.py CLI) ---
# sounddevice needs the system PortAudio library at runtime even though it
# ships a pure-Python wheel on PyPI. If deploying to a headless/server
# environment (e.g. Streamlit Cloud), add a packages.txt with
# `libportaudio2` or this import will fail at startup. app.py avoids
# importing core/audio.py directly for this reason.
sounddevice==0.5.5
soundfile==0.13.1

# --- Transitive dependencies (pinned for reproducible installs) ---
cffi==2.0.0
contourpy==1.3.0
cycler==0.12.1
fonttools==4.60.2
importlib_resources==6.5.2
kiwisolver==1.4.7
packaging==26.2
pillow==11.3.0
pycparser==2.23
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
zipp==3.23.1
```